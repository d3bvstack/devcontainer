{
  "name": "Dev Container",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
    ,"args": {
      "USER_UID": "1000",
      "USER_GID": "1000",
      "USERNAME": "vscode"
    }
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      },
      "extensions": [
        "cschlosser.doxdocgen"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends build-essential python3-pip fzf zip unzip procps iproute2 iputils-ping && sudo apt-get autoremove --purge -y && sudo apt-get autoclean && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/* /var/cache/apt/archives/* /tmp/* /var/tmp/*",
  "forwardPorts": [8000],
  "updateRemoteUserUID": true,

  "remoteUser": "vscode"
}
